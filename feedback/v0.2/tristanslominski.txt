UI feedback: the dialog covers up the main artifact. I never get to see the thing I'm working on. It would be helpful if all the dialog boxes showed up off the map so I'm never taken out of the context of seeing the map.

On the positive side, I'm glad when creating the map I don't have to type in a component name anymore but choose from precreated ones. I'd put the precreated ones as the primary interaction and remove or deemphisize the type-your-own component interaction.